# DocFX build output (regenerated by 'docfx build docfx.json'; web deploy handled by CI).
# Note: source images live in ./.attachments (tracked) -- only _site/.attachments is ignored.
_site/

# Retired mdBook build output.
book/
mdbook/

# OS / editor cruft.
.DS_Store
Thumbs.db
*.log
